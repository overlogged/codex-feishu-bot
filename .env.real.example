COMPOSE_PROJECT_NAME=codex-feishu-bot-real
CODEX_CLI_VERSION=0.153.4

# 对外暴露给宿主机的 app 端口
APP_PORT=3000
# 默认工作目录。群聊只能绑定这个根目录下的子目录
DEFAULT_WORKSPACE=/home/overlogged
# 群聊工作区映射文件
CHAT_WORKSPACE_BINDINGS_FILE=.codex-feishu-bot/chat-workspaces.json
# 用户可见的导出产物目录
CODEX_ARTIFACTS_DIR=.codex-feishu-bot/artifacts
# 运行态快照文件
RUNTIME_STATE_FILE=.codex-feishu-bot/runtime-state.json
# 宿主机 Codex 调用飞书 API / 发文件时使用的桥接脚本
FEISHU_BRIDGE_SCRIPT=scripts/feishu-bridge.mjs
# 飞书消息更新的防抖窗口，调试时可压低
LIVE_UPDATE_DEBOUNCE_MS=1200

# 额度/用量统计：ccusage 命令路径（需 npm install -g ccusage）
CCUSAGE_COMMAND=ccusage
# ccusage 统计结果的缓存时长（毫秒）
USAGE_CCUSAGE_CACHE_MS=300000
# ccusage 费用的 USD → 人民币汇率
USAGE_USD_TO_CNY_RATE=7.2
# Kimi 账号额度读取所用的本机 kimi CLI 登录凭证
KIMI_CODE_CREDENTIALS_FILE=/home/overlogged/.kimi-code/credentials/kimi-code.json
# ccusage 当天累计用量快照文件，用于估算「近 1 小时」消耗
USAGE_SNAPSHOT_FILE=.codex-feishu-bot/usage-snapshots.json
# 用量快照记录间隔（毫秒）
USAGE_SNAPSHOT_INTERVAL_MS=600000
# token 日报默认发送时间（HH:mm，本地时区）；群里可用“token 日报 23:30”单独覆盖
TOKEN_DAILY_REPORT_TIME=23:00
# codex-auth 多账号 CLI 命令（“账号”/“切账号”指令用）
CODEX_AUTH_COMMAND=codex-auth
# codex-auth 的账号 registry 文件
CODEX_AUTH_REGISTRY_FILE=/home/overlogged/.codex/accounts/registry.json

# 飞书区域，一般保持 feishu
FEISHU_DOMAIN=feishu
# 飞书长连接看门狗：SDK 自带的断线重连很慢（固定 120s 间隔），掉线期间飞书不补发事件，
# 用户消息会静默丢失。开启后掉线超过 STALE_MS 就强制重建 WS 客户端（不重启进程）。
FEISHU_WS_WATCHDOG_ENABLED=true
FEISHU_WS_WATCHDOG_INTERVAL_MS=60000
FEISHU_WS_WATCHDOG_STALE_MS=180000
# 长连接必须显式走代理：SDK 的 REST 调用（axios）会自动读 HTTPS_PROXY，
# 但 ws 包不认代理环境变量。本机只能通过代理出网时必须配置，否则长连接永远建不起来
# （现象：REST 发消息正常，但收不到任何事件）。留空则回退 HTTPS_PROXY / HTTP_PROXY。
FEISHU_WS_PROXY_URL=
# 置 true 则长连接强制直连，忽略上面的代理配置
FEISHU_WS_PROXY_DISABLED=false
# 由 Codex 通过飞书开放平台自动化配置后写回
FEISHU_APP_ID=
# 由 Codex 通过飞书开放平台自动化配置后写回
FEISHU_APP_SECRET=
FEISHU_VERIFICATION_TOKEN=

# 运行时 Codex 默认模型和思考深度；群绑定可以覆盖为 GPT-5.6 Sol 和其他支持的深度
CODEX_APP_SERVER_MODEL=gpt-6-astra
CODEX_APP_SERVER_MODEL_REASONING_EFFORT=high
# Kimi ACP 命令；host 模式通过 kimi-code 的 `kimi acp` 接入并复用 ~/.kimi-code 鉴权
KIMI_ACP_COMMAND=kimi
# DeepSeek Harness ACP 命令与 profile；`ds` / `deepseek` 默认走 `dsh --profile acp`
# 模型/provider 由 ~/.dsh/settings.yaml 的 profile 配置决定
DSH_ACP_COMMAND=dsh
DSH_ACP_PROFILE=acp
# dsh ACP 会话的 reasoning_effort（off/low/medium/high/max）；留空则不设置，模型不会输出思考过程
DSH_ACP_REASONING=high
# Pi CLI 命令与模型配置，默认连接 OpenRouter 的 DeepSeek V4 Pro；消息里指定 DS4 Flash / V4 Flash 时会切换到 Flash
PI_CLI_COMMAND=pi
PI_CLI_PROVIDER=openrouter
PI_CLI_MODEL=deepseek-v4-pro
PI_CLI_THINKING=xhigh
# yolo 默认值: never + danger-full-access
CODEX_APP_SERVER_APPROVAL_POLICY=never
CODEX_APP_SERVER_SANDBOX=danger-full-access
CODEX_APP_SERVER_LISTEN_URL=ws://127.0.0.1:4500
# false 表示 bot 不在本进程内托管启动 Codex，需先在宿主机运行 pnpm codex:host
CODEX_APP_SERVER_MANAGED=false
SLOCK_SERVER_URL=https://api.slock.ai
SLOCK_API_KEY=

# 认证方式二选一。
# 1. 推荐：宿主机已有 ~/.codex 时直接复用
CODEX_HOME_SOURCE=/home/overlogged/.codex
# 2. 或者填 OPENAI_API_KEY，宿主机首次启动时会自动 codex login
OPENAI_API_KEY=

# Only Codex / codex-auth use this outbound proxy; bot and other backends connect directly.
CODEX_PROXY_URL=
